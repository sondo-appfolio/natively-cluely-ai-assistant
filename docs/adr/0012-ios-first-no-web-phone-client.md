# iOS native app first; no web phone client product

The phone-side product is the **react-native-phone-mirror-client** (ADR 0013). We do not ship or sequence behind the Safari/HTML Phone Mirror UI. Desktop keeps PhoneMirrorService as the session host. Android is the next client effort after iOS works (`ios-first-no-web-phone-client`).
